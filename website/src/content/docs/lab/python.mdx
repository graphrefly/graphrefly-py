---
title: Python (Pyodide)
description: Run Python in the browser — foundation for multi-agent / CQRS walkthroughs.
---

import PyodidePlayground from '../../../components/PyodidePlayground';

<PyodidePlayground client:visible />

This page uses a **React island** (`client:visible`) and the **Pyodide** package with assets loaded from jsDelivr. It does not run the full `graphrefly-py` package in WASM yet; extend the sample once dependencies are available under Pyodide.
