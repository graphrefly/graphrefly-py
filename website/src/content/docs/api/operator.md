---
title: 'operator'
description: 'Custom transform: ``node(deps, fn, opts)`` — ``fn`` uses ``down()`` explicitly.'
---

Custom transform: ``node(deps, fn, opts)`` — ``fn`` uses ``down()`` explicitly.

## Signature

```python
def operator(deps: Sequence[Node[Any]], fn: NodeFn, **opts: Any) -> Node[Any]
```

## Documentation

Custom transform: ``node(deps, fn, opts)`` — ``fn`` uses ``down()`` explicitly.
