---
title: 'DictCheckpointAdapter'
description: 'Store under a fixed key inside a caller-owned ``dict`` (tests / embedding).'
---

Store under a fixed key inside a caller-owned ``dict`` (tests / embedding).

## Signature

```python
class DictCheckpointAdapter
```

## Documentation

Store under a fixed key inside a caller-owned ``dict`` (tests / embedding).
