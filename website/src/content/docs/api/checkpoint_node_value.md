---
title: 'checkpoint_node_value'
description: "Minimal JSON-shaped payload for a single node's last value (for custom adapters)."
---

Minimal JSON-shaped payload for a single node's last value (for custom adapters).

## Signature

```python
def checkpoint_node_value(node: Node[Any]) -> dict[str, Any]
```

## Documentation

Minimal JSON-shaped payload for a single node's last value (for custom adapters).
