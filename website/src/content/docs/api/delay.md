---
title: 'delay'
description: 'Delay each ``DATA`` by ``seconds`` (one timer per pending value, FIFO).'
---

Delay each ``DATA`` by ``seconds`` (one timer per pending value, FIFO).

## Signature

```python
def delay(seconds: float) -> PipeOperator
```

## Documentation

Delay each ``DATA`` by ``seconds`` (one timer per pending value, FIFO).
