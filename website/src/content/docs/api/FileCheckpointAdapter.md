---
title: 'FileCheckpointAdapter'
description: 'Atomic JSON file persistence (write temp + replace).'
---

Atomic JSON file persistence (write temp + replace).

## Signature

```python
class FileCheckpointAdapter
```

## Documentation

Atomic JSON file persistence (write temp + replace).
