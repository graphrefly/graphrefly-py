---
title: 'of'
description: 'Emit each argument as ``DATA`` in order, then ``COMPLETE`` (cold on subscribe).'
---

Emit each argument as ``DATA`` in order, then ``COMPLETE`` (cold on subscribe).

## Signature

```python
def of(*values: Any) -> Node[Any]
```

## Documentation

Emit each argument as ``DATA`` in order, then ``COMPLETE`` (cold on subscribe).
