---
title: 'buffer_time'
description: 'Emit a list of ``DATA`` values collected over each wall-clock window of ``seconds``.'
---

Emit a list of ``DATA`` values collected over each wall-clock window of ``seconds``.

## Signature

```python
def buffer_time(seconds: float) -> PipeOperator
```

## Documentation

Emit a list of ``DATA`` values collected over each wall-clock window of ``seconds``.
