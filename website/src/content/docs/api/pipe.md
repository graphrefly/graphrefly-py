---
title: 'pipe'
description: 'Linear composition; returns the last node. Does not register a Graph.'
---

Linear composition; returns the last node. Does not register a Graph.

## Signature

```python
def pipe(source: Node[Any], *ops: PipeOperator) -> Node[Any]
```

## Documentation

Linear composition; returns the last node. Does not register a Graph.
